{% extends 'base.html.twig' %}

{% block title %}Edit Lessons{% endblock %}

{% block body %}
    <div class="container">
        {{ include('lessons/_form.html.twig', {'button_label': 'Update'}) }}
        <div class="row mt-3">
            <div class="col text-end">
                <a class="btn btn-primary" href="{{ path('app_lessons_index') }}">Back to list</a>
            </div>
            <div class="col-auto">
                {{ include('lessons/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
